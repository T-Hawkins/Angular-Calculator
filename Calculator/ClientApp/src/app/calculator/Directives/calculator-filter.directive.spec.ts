import { CalculatorFilterDirective } from './calculator-filter.directive';

describe('CalculatorFilterDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CalculatorFilterDirective();
    expect(directive).toBeTruthy();
  });
});
